title: GovBook
description: Public, verified profiles of elected officials — aggregated from public records.
markdown: kramdown
theme: null   # use our custom layout, not a built-in theme
permalink: pretty

# Optional: turn blog/news into collections later
# collections:
#   blog:
#     output: true
#   news:
#     output: true

defaults:
  - scope:
      path: ""
    values:
      layout: default
